<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-gray-50 text-gray-800 font-sans">

    <nav class="bg-white shadow">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <a href="/" class="text-2xl font-bold text-blue-600">Leisure Center</a>
                <div class="flex items-center space-x-6">
                    <a href="/classes" class="text-gray-700 hover:text-blue-600 font-medium">Classes</a>
                    <a href="/workouts" class="text-gray-700 hover:text-blue-600 font-medium">Workouts</a>
                    <a href="/nutrition" class="text-blue-600 font-medium">Nutrition</a>
                </div>
            </div>
        </div>
    </nav>

    <header class="bg-gradient-to-r from-blue-600 to-green-500 text-white py-16">
        <div class="max-w-7xl mx-auto text-center">
            <h1 class="text-4xl font-extrabold mb-4">Nutrition Information</h1>
            <p class="text-lg">Search for food and get detailed nutrition information.</p>
        </div>
    </header>

    <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <h2 class="text-3xl font-bold text-gray-800 mb-6">Search for Nutrition Facts</h2>
        <form method="GET" action="/nutrition" class="mb-6">
            <input type="text" name="query" placeholder="Enter a food item (e.g., apple, banana)"
                class="w-full border rounded-lg px-4 py-2" />
            <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded mt-2">Search</button>
        </form>

        <% if (errorMessage) { %>
            <p class="text-red-500">
                <%= errorMessage %>
            </p>
            <% } else { %>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                    <% nutritionData.forEach(item=> { %>
                        <div class="bg-white rounded-lg shadow p-6">
                            <h3 class="text-lg font-bold text-blue-600 mb-2">
                                <%= item.name %>
                            </h3>
                            <p class="text-gray-500 mb-2">Brand: <%= item.brand || 'N/A' %>
                            </p>
                            <p class="text-gray-500 mb-2">Calories: <%= item.calories || 'N/A' %>
                            </p>
                            <p class="text-gray-500 mb-2">Fat: <%= item.fat || 'N/A' %> g</p>
                            <p class="text-gray-500 mb-2">Protein: <%= item.protein || 'N/A' %> g</p>
                        </div>
                        <% }); %>
                </div>
                <% } %>
    </section>

</body>

</html>